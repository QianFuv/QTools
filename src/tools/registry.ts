import type { ToolDefinition } from "../types/tool";
import homeTool from "./home";
import greeterTool from "./greeter";

const tools: ToolDefinition[] = [homeTool, greeterTool];

export default tools;
